<?xml version="1.0" encoding="utf-8" ?>
<odoo>

    <record id="view_quotation_tree" model="ir.ui.view">
        <field name="name">sale.order.list</field>
        <field name="model">sale.order</field>
        <field name="inherit_id" ref="sale.view_quotation_tree" />
        <field name="arch" type="xml">
            <field name="invoice_status" position="after">
                <field name="provider_id" optional="hide" />
                <field name="payment_status" optional="show" />
            </field>
        </field>
    </record>

    <record id="view_order_tree" model="ir.ui.view">
        <field name="name">sale.order.list</field>
        <field name="model">sale.order</field>
        <field name="inherit_id" ref="sale.view_order_tree" />
        <field name="arch" type="xml">
            <field name="invoice_status" position="after">
                <field name="provider_id" optional="hide" />
                <field name="payment_status" optional="show" />
            </field>
        </field>
    </record>


    <record id="view_order_form" model="ir.ui.view">
        <field name="name">sale.order.form</field>
        <field name="model">sale.order</field>
        <field name="inherit_id" ref="sale.view_order_form" />
        <field name="arch" type="xml">
            <xpath expr="//field[@name='payment_term_id']" position="after">
                <label for="payment_amount" string="Payment" />
                <div class="o_row">
                    <field
                        name="payment_amount"
                        decoration-success="payment_status == 'done'"
                        decoration-warning="payment_status in ('partial', 'pending', 'initiated', 'authorized')"
                        decoration-danger="payment_status == 'cancelled'"
                    />
                    <field name="provider_id" widget="many2one" options="{'no_create': True, 'no_open': True}" />
                </div>
                <field
                    name="payment_status"
                    decoration-success="payment_status == 'done'"
                    decoration-warning="payment_status in ('partial', 'pending', 'initiated', 'authorized')"
                    decoration-danger="payment_status == 'cancelled'"
                    decoration-muted="payment_status == 'without'"
                />
            </xpath>
        </field>
    </record>


    <record id="view_sales_order_filter" model="ir.ui.view">
        <field name="name">sale.order.list.select</field>
        <field name="model">sale.order</field>
        <field name="priority">50</field>
        <field name="inherit_id" ref="sale.view_sales_order_filter" />
        <field name="arch" type="xml">
            <filter name="my_sale_orders_filter" position="before">
                <filter string="Fără plată" name="payment_without" domain="[('payment_status', '=', 'without')]" />
                <filter
                    string="Plată inițiată"
                    name="payment_initiated"
                    domain="[('payment_status', '=', 'initiated')]"
                />
                <filter
                    string="Plată în așteptare"
                    name="payment_pending"
                    domain="[('payment_status', '=', 'pending')]"
                />
                <filter
                    string="Plată autorizată"
                    name="payment_authorized"
                    domain="[('payment_status', '=', 'authorized')]"
                />
                <filter string="Plată efectuată" name="payment_done" domain="[('payment_status', '=', 'done')]" />
                <filter
                    string="Plată anulată"
                    name="payment_cancelled"
                    domain="[('payment_status', '=', 'cancelled')]"
                />
                <separator />
            </filter>
        </field>
    </record>

</odoo>
