1. Go to **Settings > General Settings > Companies**, open a company record and set the **Price List Currency** field (defaults to EUR).
2. Products belonging to that company will compute their **List Price** currency from this field instead of always using the company's main currency, so a product's list price can be expressed in a different currency than the one used for accounting.
