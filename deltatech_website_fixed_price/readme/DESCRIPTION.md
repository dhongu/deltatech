- Features:

  - The comparison price will show even if you use fixed price in pricelist rules
  - By default, odoo will not show the slashed price if the fixed price is bigger than the comparison price
