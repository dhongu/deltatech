Features:
 - adds two grouping elements for internal categories: "Category type" and "Category class"
 - reports that can group by the two elements: Sale margin report, Stock quant, Account invoice report
