1. Go to **Point of Sale > Orders > Orders**.
2. Open the search bar and type (or filter by) a product name/reference.
3. The search now matches against the products on the order lines, not just the customer, so you can find every POS order that contains a given product.
