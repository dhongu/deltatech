No configuration is needed. Once installed:

- On the Customer Portal, the **My Quotations** and **My Orders** lists show an extra **Reference** column with the customer's own order reference (Client Reference).
- Both lists gain a search box that lets the portal user choose where to search — **Order Name**, **Client Reference**, or **All** — via a search-in dropdown, instead of only searching by order name.
