<odoo>

      <!-- Extend Stock Location Form View to add valuation_area_id field -->
    <record id="view_location_form_valuation_area" model="ir.ui.view">
        <field name="name">stock.location.form.valuation.area</field>
        <field name="model">stock.location</field>
        <field name="inherit_id" ref="stock.view_location_form" />
        <field name="arch" type="xml">
            <xpath expr="//group[@name='additional_info']" position="inside">
                <field name="valuation_area_id" options="{'no_create': True}" groups="stock.group_stock_manager" />
            </xpath>
        </field>
    </record>

</odoo>
