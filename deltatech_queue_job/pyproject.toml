[build-system]
requires = [
    "whool",
]
build-backend = "whool.buildapi"

[project]
name = "odoo-addon-deltatech-queue-job"


[project.optional-dependencies]
odoo = [
    "odoo-addon-queue-job-cron-jobrunner @ git+https://github.com/OCA/queue.git@18.0#subdirectory=queue_job_cron_jobrunner",
]
