[build-system]
requires = [
    "whool",
]
build-backend = "whool.buildapi"

[project]
name = "odoo-addon-deltatech-queue-job"



[project.optional-dependencies]
odoo = [
    "queue_job_cron_jobrunner @ git+https://github.com/OCA/queue.git@18.0#egg=queue_job_cron_jobrunner",
]
