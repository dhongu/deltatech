No configuration is needed. Once installed, the attribute filters shown in the shop's sidebar and off-canvas filter menu (select dropdowns, radio buttons, pills, checkboxes/multi-select and color swatches) are automatically limited to the attribute values that actually belong to the products currently displayed (after a search or when browsing a category), instead of showing every attribute value that exists in the system. The filters refresh automatically whenever the visitor searches or changes category.
