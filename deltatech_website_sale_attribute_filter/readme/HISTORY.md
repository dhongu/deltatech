## 19.0.0.1.0

- Preserve the attribute filter UI state across the page reload that Odoo
  triggers when a filter value is selected: expanded attribute accordions stay
  open, the mobile off-canvas filter panel reopens automatically, and the scroll
  position is restored. The customer no longer has to reopen the filter
  selectors after every selection.
