This is a pure UI behavior fix with no configuration or setup required.
Once installed, it automatically changes list (tree) view behavior: if
you click-drag to select text inside a list cell, the row will no
longer open the record. Clicking normally (without selecting text)
still opens the record as before.
