<?xml version="1.0" encoding="utf-8" ?>
<odoo>
    <record id="res_config_settings_view_form" model="ir.ui.view">
        <field name="name">res.config.settings.view.form</field>
        <field name="model">res.config.settings</field>
        <field name="priority" eval="11" />
        <field name="inherit_id" ref="base_setup.res_config_settings_view_form" />
        <field name="arch" type="xml">
            <xpath expr="//block[@name='companies_setting_container']" position="inside">
                <setting
                    id="is_productive_system"
                    string="Database Is Neutralized"
                    help="This setting is used to determine if the database is a test or production system."
                >
                    <field name="database_is_neutralized" />

                    <div class="text-muted" invisible="not database_is_neutralized">
                        It is a test system
                    </div>
                    <div class="text-muted" invisible="database_is_neutralized">
                        It is a productive system
                    </div>
                </setting>

            </xpath>
        </field>
    </record>
</odoo>
