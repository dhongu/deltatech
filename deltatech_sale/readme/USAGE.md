This module works automatically, with no configuration:

- On a quotation, if you try to add a product line before selecting a **Customer**, you get a blocking message asking you to pick the customer first.
- The **Sales Analysis** report's margin/profitability figures are computed using the product's **standard price** (cost) instead of the default Odoo margin calculation.
