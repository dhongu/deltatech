This module only extends the Sales Analysis report; there is no setting to configure.

- Go to **Sales > Reporting > Sales**. The report (list, pivot, and graph views) now includes the customer's **Email** as an additional field/measure, so orders can be grouped, filtered, or exported by customer email.
