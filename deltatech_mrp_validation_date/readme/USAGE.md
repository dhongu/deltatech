This module is fully automatic and requires no configuration. On any Manufacturing Order form (**Manufacturing > Operations > Manufacturing Orders**), a read-only **Validation Date** field appears next to the responsible user. It is filled in automatically with today's date the moment the order is marked as done (**Mark as Done** button), so you can see and report on when each production was actually completed.
